<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:windowBackground">@color/color_secondary</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:typeface">serif</item>
    </style>

    <style name="ToolbarCustomization" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:dropDownListViewStyle">@style/PopupMenuStyle</item>
        <item name="dropDownListViewStyle">@style/PopupMenuStyle</item>
        <!--Menu TextColor-->
        <item name="android:textColor">@color/color_primary_dark</item>
    </style>

    <style name="PopupMenuStyle" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">@color/color_grey</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Spinner.Main" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CalendarHeader">
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/color_primary_text</item>
        <item name="android:textSize">@dimen/larger_text_size</item>
    </style>

    <style name="CalendarDay">
        <item name="android:textColor">@color/color_primary_text</item>
    </style>

</resources>
